{% extends '@PubLeash/layout.html.twig' %}

{% block body %}
    Publication
    {{ dump(paginator) }}

    {% for publication in paginator.iterator %}
        {{ dump(publication) }}
    {% endfor %}
{% endblock %}
